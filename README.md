# kafka-rs-consumer
A reusable Kafka consumer framework that handles the boilerplate of consumer group management, offset commits, dead-letter queues, and error handling letting users plug in their own message handler logic.
